// ============================================================
// 文件名: Config.h
// 模块:   基础驱动/全局配置
// 职责:   编译时全局常量——固件版本号、防除零保护值等跨模块共享的配置项
// 关键类/函数:
//   - FIRMWARE_VERSION: 当前固件版本字符串
//   - MIN_VALID_DIFF: 信号处理防除零保护值（与前端 JS 一致，mV 单位下对应 0.5f）
// ============================================================
#pragma once
// 防止版本失控与时序漂移
#define FIRMWARE_VERSION "1.0.0"

// 防除零保护，与前端 JS 绝对一致 (当前 mV 单位下对应 0.5f)
#define MIN_VALID_DIFF 0.5f
